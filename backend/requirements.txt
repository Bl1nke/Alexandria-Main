# =============================================================================
# ЗАВИСИМОСТИ ПРОЕКТА "ALEXANDRIA"
# Установка: pip install -r requirements.txt
# =============================================================================

# --- Веб-фреймворк ---
flask==3.0.0
flask-cors==4.0.0
werkzeug==3.0.1

# --- Работа с PDF ---
# pypdf: извлечение текста из PDF
pypdf==3.17.0
# PyMuPDF (fitz): конвертация PDF-страниц в изображения (высокая скорость)
PyMuPDF==1.24.0

# --- Работа с DOCX ---
python-docx==1.1.0

# --- Обработка изображений ---
# Pillow: создание заглушек-изображений для DOCX-файлов
Pillow==10.1.0
# --- Утилиты (опционально, но рекомендуется) ---
# python-dotenv: для загрузки переменных окружения из .env файла
python-dotenv==1.0.0
# gunicorn: WSGI-сервер для запуска в продакшене (Linux/Mac)
gunicorn==26.0.0; platform_system != "Windows"
# waitress: аналог gunicorn для Windows
waitress==2.1.2; platform_system == "Windows"
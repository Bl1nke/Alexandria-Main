# ═══════════════════════════════════════════════
# ALEXANDRIA — зависимости
# Установка: pip install -r requirements.txt
# ═══════════════════════════════════════════════

# Веб-фреймворк
flask==3.0.0
flask-cors==4.0.0
werkzeug==3.0.1

# PDF: извлечение текста + рендер страниц в изображения
PyMuPDF==1.24.0         # fitz — основная библиотека

# DOCX
python-docx==1.1.0

# Изображения
Pillow==10.1.0

# Переменные окружения (опционально)
python-dotenv==1.0.0

# WSGI-сервер (продакшен)
gunicorn==21.2.0; platform_system != "Windows"
waitress==3.0.0; platform_system == "Windows"